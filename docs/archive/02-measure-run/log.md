> **ARCHIVED HISTORICAL LOG — 2026-07-14.** Milestone 02 and S8 are shipped.
> Chronological references below to future slices, H2 collection, public-first work,
> portfolio assets, or user-paste gates record past decisions and are not current
> instructions. H2 is final, GitHub is the complete showcase surface, milestone 03
> remains parked, and the Task 7 private/public release gates remain in force.

2026-07-08 — milestone reframed (spec committed 2ae9109): tiered verification replaces second labeler; discovered during planning: (1) claim-source vocabulary was never pinned — provenance is LLM free-text, pinned this milestone; (2) T1-amendment-draft requires user-pasted §9 entries (builder never edits the prereg); (3) claim_id embeds the arm — blinding needs opaque ids.
- **2026-07-08 — Amendment 1 logged (user-delegated paste at Gate 1).** Spec exit criterion updated: §9 carries TWO entries this milestone (reframe amendment + T1 instrument pin), not one — the T1-pin machinery (docs/archive/01-end-to-end/T1-amendment-draft.md, user-pastes-only, SHA must postdate the reframe's instrument edits) was discovered at planning, after the spec froze its wording. Referee hardenings folded before paste: §8 actions-vs-interpretation wording, cross-tier rate assembly pinned (label_tier1 else adjudicated R1), pairing citation-resolution caveat + blind-check control, judge-id live-verification note, in-body supersession restatement.
- **2026-07-08 — Tier-1 "resolvable" reading pinned (pre-data).** Implementation note on Amendment 1's pairing rule: the verifier's universe is the supplied top-K list (the amendment's mechanism sentence), so an *unresolvable/fabricated* `pairing:<name>` citation is machine-labeled `grounded-mischaracterized`, not sent to Tier 2 — the mischaracterization rate can absorb a small number of true hallucinations, bounded by the verifier↔author blind-check control. Recorded before any eval data; to restate at the T1-pin gate.
- **2026-07-08 — DeepSeek re-verify.** SPEC §4c: cosmetic drift patched per CLAUDE.md gotcha; no structural drift; judge model chosen deepseek-v4-flash (D3 satisfied — different model, same family, caveat stands).
- **2026-07-09 — S1–S4 shipped; stub Tier-1 dry-run baseline.** `make test`/`vet`/`build` green. Ratified 13-seed benchmark through the stub runner: 39 unique claims/arm (117 total) after (text,source) dedup — well below the ~195 move-slot estimate because the stub's per-template claims collapse hard on dedup. Tier-1 labels 100% of stub claims (0 fall-through) since the stub only emits `pairing:`/empty-source citations, both mechanically decidable. Live DeepSeek (S5) will emit `fdc:`/`foodon:`/free-text provenance → real Tier-2 fall-through and author labeling load are unknown until then; the per-arm coverage line at live-run exit is the S3-exit coverage flag. §9 T1 instrument freeze pinned at commit 08903cb (7 paths diff-empty to HEAD).
- **2026-07-09 — S1–S4 final whole-branch review: SHIP.** Opus reviewer over b784160..a388e5d (37 commits): no Critical/Important findings. Verified — PREREGISTRATION §1–§8 byte-unchanged (only §9 additions; sole deletion = the placeholder row); Tier-1 rule set matches Amendment 1 verbatim; per-move evidence derivation has no off-by-one (re-derived == generation-time); blinding structurally leak-free; judge enum enforced on strict + json_object paths; shared budget meter; no llm→eval import cycle; tests non-vacuous. **Pre-live watch note (carried minor #1):** evidence.tmpl uses bare `<foodon_id>` for citations[].ref but prefixed `foodon:<id>` for flavor_rationale[].provenance — self-consistent (verifier reads provenance only; a bare foodon in provenance falls to Tier 2, never mislabels), but since internal/llm/prompts/ is now T1-frozen, inspect the S5 first batch's grounded-arm foodon provenance formatting BEFORE the full spend; any fix needs a fresh instrument pin + §9 note. Minors #2–#4 deferred (stub-only / S6-forward-dep / contrived).
- **2026-07-09 — pre-S5 spec gap found + fixed: eval runs were untraced; api-docs NO DRIFT.** `buildDeps` wired deps "minus telemetry" — `eval run --live` would have emitted zero Langfuse traces, contradicting the spec's "runs traced OTel→Langfuse" (Part 2) and the handoff's trace step. Fixed by wiring `telemetry.Setup` into `buildDeps` (cmd/eval only; all 7 frozen instrument paths untouched, freeze diff re-verified empty). Fresh-context review SHIP with one Important finding, accepted + fixed: tracing is gated on `--live` — stub dry-runs never export (the Makefile auto-exports `.env`, whose real LANGFUSE keys would have silently mixed harness spans into the operator trace store; reviewer reproduced the pollution live). Caveat: one intermediate `make test` ran before the gating landed — a handful of stray stub-test spans (service `capycook`, `llm.generate_move`) may sit in Langfuse dated 2026-07-09; ignore/delete on sight. Same gate: live api-docs re-verify (Amendment 1 commitment) → **NO DRIFT** — `deepseek-v4-pro`/`deepseek-v4-flash` confirmed, pricing matches SPEC §4c to the cent, `/beta` strict-mode + `json_object` caveats unchanged, 07-24 legacy-alias deprecation touches neither v4 id; §4c strict-mode doc-drift watch item resolved in the repo's favor.
- **2026-07-10 — S5 SHIPPED: live 3-arm campaign complete.** Three hardening rounds preceded the successful run, each root-caused (never guessed), fresh-context-reviewed CONFIRM SHIP, and committed separately: (1) stub-era 30s move wait vs 3×60s legal client worst-case → `--move-timeout` flag + 120s CallTimeout (d618f20); (2) sticky fallback demotion re-rolling unenforced json_object dice after one strict misfire → bounce-back to the server-enforced strict path + per-attempt logging + payload-snippet diagnostics (5a1c459) — live evidence: strict path stochastically emits syntactically invalid tool args (missing comma) on long-content moves, contradicting the /beta strict-mode docs; (3) the v1 all-or-nothing abort policy (validated only against the stub) meeting ~5–11% observed per-move abort risk × 65 moves → **Amendment 2** (user-gated): bounded move retries (regenerate on block, re-propose on fail, limit 3, whole-seed skip loudly reported) + generator client bound 2→4; instruments re-pinned at `32afe54`, §9 entry user-delegated-pasted (7dd5c51), §1–§8 byte-unchanged. **Results of the counted run:** ungrounded 150 / flavorgraph 203 / grounded 209 claims (562 total); seeds 12/13 in EVERY arm — bench-12 (tree-nuts stress seed) blocked all 4 generations in all 3 arms on the same deterministic `allergen-unresolved` (short pasta) rule: perfectly symmetric, no cross-arm selection bias, but the seed is un-runnable as generated (S6 writeup item). **Tier-1 coverage 100% on all arms — zero Tier-2 claims.** Provenance census: ungrounded all-empty; flavorgraph/grounded each 10 `pairing:` (all resolved → grounded-correct) + rest empty; ZERO fdc:/foodon:/free-text tokens, zero mischaracterizations — the pre-live foodon-formatting watch note is moot. Consequences, recorded before any human labeling: blinded R1 sheet is vacuous (`eval: label sheet: no Tier-2 claims` — machine-refused, not silently empty); judge R2 ran and confirmed `562 tier-1 (not judged), 0 labeled` at $0; κ/confusion-matrix has no double-labeled rows to chew on (S6 must report this plainly as the frozen machinery meeting unexpected data shape, never dress it up); author human-labeling load = ONLY the Tier-1 blind-check control (18-row stratified sample exported, map sealed until the blind pass). Citation uptake is low (10/209, 10/203, 0/150) — the H1 contrast rests on citation-bearing claims and the findings paragraph must own that honestly. Langfuse: 220 `llm.generate_move` traces verified via API (Phase-0 wiring works in production). Total spend $0.87 of the $2.00 cap, including all failed attempts and diagnostics. Artifacts backed up in `eval/out/live-backup-2026-07-09/` (gitignored).
- **2026-07-10 — S6 carried-notes triage: all four remaining items confirmed non-live-data-affecting, no fix opened.** Task 13 note (adjudication must write author-final into `label_r1` before rates) — same item as deferred **minor "S6-forward-dep"**: **moot** — S5 landed zero Tier-2 claims (line above), so adjudication never runs; `FinalLabel()` (`internal/eval/rates.go:46-51`) reads `label_tier1` for all 562 live claims, `label_r1` is never populated for live data. Task 6 note (README "tiered labeling kit" wording): **already done** — `grep -n "tiered labeling kit" README.md` → one hit at `README.md:133`. Task 12 note (appended claims share one `*string` ref) — deferred **minor "stub-only"**: confirmed stub-only — `internal/llm/stub.go:46-49` aliases a single `ref` across every claim a stub move appends; the live campaign generated via the real DeepSeek client, never `Stub.GenerateMove`, so the aliasing never touched the 562 live claims. Deferred **minor "contrived"** (Task 5: pipe-less table rows / non-% proportions not caught by `TestREADMEResultsTableEmpty`'s guard): inspected — a doc-guard formatting edge case, no bearing on computed rates or claim data; left as-is, no fix opened. All four inspected, none live-data-affecting; none escalated.
- **2026-07-10 — Tier-1 blind-check scored (rater per §9 Amendment 3: author-delegated LLM, fresh-context Claude agent).** 15/18 (83%) delegate↔verifier agreement over 18 stratified rows; confusion: correctly-unverified→correctly-unverified 15, correctly-unverified→opinion-non-checkable 3. All three divergences are subjective flavor-harmony rows the rater judged non-checkable where the mechanical empty-source rule says correctly-unverified — the residual-risk shape Amendment 1's control watches for, reported as signal. Sequence: sheet filled by the delegate 13:45, integrity-verified against the sealed original (dish/text/source byte-identical, labels in frozen vocabulary), Amendment 3 pasted (user-delegated, 09c66c6), THEN map unsealed by the scorer. Scorer CLI prints "verifier↔author" — legacy Amendment-1 wording; rater of record is the LLM delegate. Control for Amendment-1's Tier-1 machine labels; agreement figure is model-validates-machine per Amendment 3's reporting rule.
- **2026-07-10 — S6 SHIPPED: Results live, Amendment 3 recorded, control scored.** 8 content commits (3a2f49d..19b2a68): plan amended per the user discussion gate (explicit H1 verdict, bench-12 inheritance note, Task-5 resequence off the critical path); carried notes triaged, none live-data-affecting; eval-pipeline diagram landed as the 3rd architecture diagram; **§9 Amendment 3** (author-delegated LLM blind-check rater — the author had the sheet filled by a fresh-context agent and adopted the labels; rater identity recorded via user-delegated paste 09c66c6 BEFORE scoring, outcome-knowledge disclosed in the entry itself); control scored **15/18 (83%)**, all three divergences correctly-unverified→opinion-non-checkable (the residual-risk shape Amendment 1's control watches for, reported as signal); README Methodology reconciled to Amendments 1–3 and Results filled with the landed numbers + honest H2 placeholder; results anti-fabrication guard retuned to pin the landed digits (oracle edit disclosed, RED→GREEN evidence on record). Whole-slice fresh-context review verdict SHIP: every figure reconciles across README/log/§9/plan, §1–§8 provably byte-untouched (45 insertions, 0 deletions), §8 honesty audit passed in full. make test/vet green; frozen-instruments diff vs 32afe54 empty. H2 fold (Task 5) pends pre-publish; S7 media wave next.
- **2026-07-10 — S7 SHIPPED: media wave.** 8 commits (4f9c438..245c969). Three user-ratified deviations, decided at session start before any work: (1) **GIFs 01–04 re-recorded** — the Jul-7 originals predate the 02a redesign and `demo.mjs`'s selectors no longer existed in the shipped UI; the rig was reconciled to the `shots.mjs` selector vocabulary, and scenes 02–08 now pre-build their dish over the HTTP API and open the workbench BEFORE recording (preroll) so each GIF opens on its subject. (2) **GIF 05 reframed** "branch → compare → promote" → "branch + promote" (`05-branch-promote.gif`) — no branch-vs-branch compare view exists until milestone 03; the scene shows snapshot → Promote to trunk → new move → Branch badge. (3) **`CAPYCOOK_STUB_LATENCY_MS`** (TDD, `internal/llm/stub.go` + config + server wiring) makes the proposing state filmable for the cancel scene: context-aware wait (Stop genuinely cancels), off by default, `cmd/eval` constructs `Stub{}` bare so eval paths can never pick it up; frozen-instruments diff vs `32afe54` re-verified empty. Encoding lesson recorded in `web/tools/README.md`: full-frame changes dominate GIF size and dithering triples them — the first restart cut was **8.5 MB**; `paletteuse=dither=none` + preroll + lossless `gifsicle -O3` landed all 9 GIFs at 800px / 5.7–11.9s / ≤0.36 MB (`--lossy` rejected: visible ghosting on the dark theme). Also shipped: 09-eval-run.gif (vhs replay of the REAL grounded-arm log via `head -5`/`tail -5`, tape committed), 3 diagram SVG exports (deliberately unreferenced by README — GitHub renders the live Mermaid; portfolio/social use), `langfuse-trace.png` (one live-campaign `llm.generate_move` span, attributes-only, cropped clean of project id/identity — captured via browser automation + gif_creator export after `save_to_disk` yielded no path), hero + 1280×640 social preview composed from the 02a gate-on-dish evidence shot in the app's own tokens, 9 portfolio MP4s. README: Demo 4→8 captures, eval replay embedded above Results, telemetry span + caption in Methodology (payloads never ride the telemetry), hero at top, stale "eval campaign in progress" status corrected to complete. **Fresh-context review: NO-SHIP on first pass** — 09-eval-run.gif ran 17.68s against the ≤15s ceiling (builder's own check had covered size/width but not duration); re-cut to 11.88s, MP4 re-exported, **re-review verdict SHIP** with independent re-measurement (ffprobe + PIL frame-sum) and full regression re-run. `make test`/`vet` green; PREREGISTRATION byte-untouched throughout.
- **2026-07-14 — Milestone 02 CLOSED at 6/6; repository PUBLIC.** The last open exit criterion
  ("CI frozen-doc guard still green") was false only for the tag path. Root cause, from the
  checkout log of run `29359723939`: `actions/checkout` force-writes `+<peeled-sha>:refs/tags/<tag>`
  whenever `rev-parse refs/tags/<tag>` (the tag object) differs from `github.sha` (the peeled
  commit) — always true for an annotated tag — so the guard read a lightweight ref and failed. The
  repository was never wrong; the workspace was. The `Frozen repository integrity` job, added in
  `02cab51` and patched three times blind, had never once passed on a tag. Fixed by deleting the
  event-payload layer entirely: `GITHUB_SHA` already *is* the target on every event, and the real
  guarantees are pin-based. This also un-gated `checkRepositoryIntegrity` — the PREREGISTRATION
  freeze had been sitting behind a door that never opened on a tag, and now runs unconditionally.
  Branch CI `29363293905` and tag CI `29363529401` both green; tag force-moved to `90c60a0` under
  USER authorization. Independently reviewed before merge. Repository made public and pinned under
  explicit USER authorization; social preview upload remains manual (no GitHub API).
- **2026-07-14 — S8 and milestone 02 SHIPPED locally.** The private `master` release candidate was archived atomically with its live oracle consumers, the exact USER-authorized administrative PREREGISTRATION relocation row, and final handoffs. GitHub itself and the nine packaged current GIF/MP4 pairs are the complete showcase surface; there is no external portfolio-site work. H2 remains final at N=2 decisions across one session. Milestone 03 stays parked behind a fresh USER go/no-go, leaving no active milestone. Task 7 (private release) was independently reviewed and approved, and the ship commit is pushed to private `origin/master`; public visibility remains separately USER-gated.
