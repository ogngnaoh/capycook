# Handoff — milestone 02 (measure-run)

## Next session start here
S7 media wave, plan Task 7 (`docs/superpowers/plans/2026-07-10-milestone-02-s6-s8.md`):
read `web/tools/README.md`, re-run the rig for one existing GIF (stub mode, no live
spend), then produce GIFs 05–08. Author-hours in parallel: accumulate real operator
sessions in the workbench (`make run`, real dishes) toward the ~8 floor — the
resequenced Task 5 folds them into README just before the S8 publish.

## Current state
- Branch `measure-run`, not pushed (D7). Tree clean; last content commit 19b2a68,
  ship-ritual commit on top.
- S1–S6 shipped. S6 (2026-07-10): README Results filled from the 562-claim campaign
  (rates at ceiling, explicit H1 null-via-ceiling verdict), eval-pipeline diagram
  (3rd), blind-check control scored 15/18 (83%) — rater = author-delegated LLM per §9
  Amendment 3 (user-delegated paste); results guard retuned to pin the landed digits;
  whole-slice fresh-context review verdict SHIP.
- PREREGISTRATION §9 now carries Amendments 1–3; §1–§8 byte-unchanged.
- H2 operator sessions still N=0; Task 5 resequenced after Task 10, before Task 11.

## Active concerns
- S8 publish gate wants an honest H2 N — the user decides when to stop accumulating.
- S7 media constraints: GIFs ≤15s / <5MB / 640–800px / 15fps; Langfuse screenshot
  needs project-id/key redaction before commit.
- Carried to S8 exit audit (optional tighten): README "all four generation attempts"
  vs Amendment-2 "limit 3 fresh" phrasing — reconciles as 1 initial + 3 retries.
